{
  "id": "GR-STAB-STD-01",
  "name": "No secrets in context",
  "description": "File paths matching secret patterns (.env, *.key, *.pem, *.crt, *.pfx, *.keystore) must never be returned by context engine or file read handler.",
  "trigger": "context_read_file | context_select_index_records | agent_stream_bridge",
  "condition": {
    "pattern_match": {
      "path": ["/\\.env(\\..*)?$/", "/\\.(key|pem|crt|pfx|keystore)$/"],
      "action": "reject"
    }
  },
  "enforcement": "blocking",
  "severity": "critical",
  "enabled": true
}
