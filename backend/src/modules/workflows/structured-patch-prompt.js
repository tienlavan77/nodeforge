/** Shared contract for deterministic structured patches. */
export const STRUCTURED_PATCH_PROMPT = "Use the NodeForge Code Index before requesting context.\nUse File Service for every file read/write. Keep changes strictly within ticket scope.\n\nReturn submit_code_response using the provided schema. Include every approved NEW and MODIFY path exactly once; READ_ONLY paths must not be returned.\n\nFor NEW files:\n- format = \\\"full_content\\\"\n- content = the complete final file content as a string\n- exists = false and before_checksum = null\n\nFor MODIFY files, Node provides the complete current file content as context. Return:\n- format = \\\"structured_patch\\\"\n- content = an object with a non-empty operations array\n- exists = true and before_checksum copied exactly from Node-provided context\n- each operation must use only its exact operation-specific fields (replace_range is exactly {op, expected_content, new_content})\n\nDo not use line numbers, offsets, placeholders, or fields not defined in the schema. Preserve every character, whitespace, newline, quote, JSX attribute, and SVG path from the provided context unless the approved change requires changing it. Never guess, reconstruct, normalize, or shorten source context.\n\nNode owns validation, checksum verification, syntax checking, and filesystem writes.";
