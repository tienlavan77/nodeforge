/** Shared contract for deterministic structured patches. */
export const STRUCTURED_PATCH_PROMPT = "Use the NodeForge Code Index before requesting context.\nUse File Service for every file read/write. Keep changes strictly within ticket scope.\n\nReturn submit_code_response using the provided schema.\n\nFor existing files:\n- format = \"structured_patch\"\n- content = { operations: [...] }\n- copy before_checksum exactly from Node-provided context.\n- include each path exactly once and combine all operations for that file.\n\nFor new files:\n- format = \"full_content\"\n- content = complete file content as a string\n- before_checksum = null\n- include a one-line summary.\n\nFor structured_patch, use only:\n- replace_range(op, expected_content, new_content)\n- delete_range(op, expected_content)\n- insert_after(op, anchor_text, new_content)\n- insert_at_end(op, new_content)\n\nEach operation must contain exactly the fields listed for its operation. Omit unused fields completely; never send them as null.\nCopy expected_content and anchor_text verbatim from the current file provided by Node. Preserve every character, whitespace, newline, quote, JSX attribute, and SVG path. Never guess, reconstruct, normalize, or shorten source context.\n\nApply operations sequentially against the current in-memory file. If exact context is unavailable or ambiguous, return code_needed.\n\nDo not use placeholders or line numbers. Node owns validation, checksum, patch application, syntax checking, and filesystem writes.";
